// Alias: /intsoft.com.br responde a mesma página que /intsoft, para o link
// escrito como domínio funcionar dentro do próprio site.
import IntSoftPage, { metadata } from "../intsoft/page";

export { metadata };
export default IntSoftPage;
